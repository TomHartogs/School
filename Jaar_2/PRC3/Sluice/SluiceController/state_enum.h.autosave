#pragma once

enum VALVETYPE
{
  Normal,
  OneSecond
};

enum  STATE
{
  Opened,
  Closed,
  Opening,
  Closing
};

enum EVENT
{
  StartSchutting,
  Open,
  Close,
  Stop
};

enum SIDE
{
  left,
  right
};

enum VALVE_STATE
{};

enum POS
{
  bottom,
  middle,
  top
};


